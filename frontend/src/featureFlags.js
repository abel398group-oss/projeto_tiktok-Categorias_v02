/**
 * Interruptores do painel.
 *
 * O painel foi enxugado para duas telas de uso diário: Categorias (coleta) e
 * Ranking (o que vale promover). As telas antigas continuam no código e
 * acessíveis por URL directa — só saíram do menu.
 *
 * Para as trazer de volta, mude para `true`.
 */
export const SHOW_LEGACY_NAV = false;
