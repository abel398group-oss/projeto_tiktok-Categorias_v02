# Chave igual a ANALYTICS_API_KEY na raiz (.env); nos exemplos: uma-chave-local
VITE_ANALYTICS_API_KEY=uma-chave-local

# Opcional: URL absoluta da API (precisa CORS no Fastify). Omitir = usar proxy do Vite.
# VITE_API_URL=http://127.0.0.1:3333
