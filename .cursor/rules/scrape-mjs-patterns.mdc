---
description: Padrões ao editar o scraper (normalizeItem, saídas, env)
globs: src/scrapeCategory.mjs
alwaysApply: false
---

# scrapeCategory.mjs

- Novos campos de produto: preferir reutilizar **`normalizeItem`** e **`toDadosProdutoClean`**, a não ser que a API exija caminho totalmente distinto.  
- URLs: **`pickProductPdpUrl`** — preferir `seo_url.canonical_url`; região do fallback a partir de `CATEGORY_URL` / `pathRegionFromCategoryUrl`.  
- Mapeamento: **`Map` por `product_id`** com **`mergeProductById`** (linha “mais rica”); nós de review têm `review_id` e são ignorados em **`normalizeItem`**. Não reintroduzir dedupe por `sku` sozinho.  
- **Regressão:** após alterar preço, merge ou filtro de review, correr **`npm test`**. Contratos vivem em **`test/scrape-regression.test.mjs`**; exportações de teste no fim de `scrapeCategory.mjs` não remover sem atualizar testes.  
- **Desconto %:** não exportar (omitir chaves de desconto em `dados_produtos` e no objeto de `normalizeItem` / `teste_categoria`); manter lógica de preço, loja, dedupe.  
- **Fotos PDP (opcional):** `PDP_GALLERY=1` e opcional `PDP_GALLERY_MAX` (padrão 25) — abre `product_url` e grava `images_pdp` / `fotos_pdp` (DOM `img`/`source` + `__MODERN_ROUTER_DATA__`); a grelha continua a preencher `images` / `fotos`.  
- **Avaliações (produto):** `rate_info` → `review_avg` / `review_count_total` / `review_star_votes` em `normalizeItem`; `toDadosProdutoClean` → `avaliacao_media` / `avaliacoes_total` / `votos_por_estrela`. No **`mergeProductById`**, as avaliações de ambas as linhas são fundidas (preenche o que faltar) sem mudar a regra de “linha mais rica” do preço.  
- Conferir ficheiros em `OUT_DIR` (`output/`) e **não** adicionar paths hardcoded fora de `package.json` / constantes do topo.  
- Env documentados: ver `docs/ARCHITECTURE.md` (tabela).
