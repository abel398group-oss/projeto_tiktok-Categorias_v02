# build/deps
node_modules/
dist/

# env/local
.env
.env.local
.env.*.local
frontend/.env.production.local
.chrome-tiktok-profile/
.puppeteer-profile/

# outputs de coleta
output/
!output/.gitkeep
!output/extra/.gitkeep

# kits exportados localmente (imagens + metadata por produto)
exportado/

# debug/logs
*.log
debug_*.log
debug_snapshots/

# OS
.DS_Store
Thumbs.db

# Playwright (E2E)
frontend/test-results/
frontend/playwright-report/
frontend/blob-report/
frontend/.playwright/

# Scripts temporários de sessão (vigias, testes pontuais)
*.tmp.sh
*.tmp.mjs

# sentinela de parada (runtime, nunca versionada)
PARAR

# lotes de curadoria (entram e saem, nao sao codigo)
curadoria/

# pacotes gerados para o Symphony (derivados, descartaveis)
exportado/
